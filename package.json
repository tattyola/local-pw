{
  "name": "local-pw",
  "version": "1.0.0",
  "description": "Playwright e2e tests for localcoding.us",
  "scripts": {
    "test": "npx playwright test",
    "test:debug": "npx playwright test --headed",
    "report": "playwright show-report",
    "codgen": "npx playwright codegen",
    "format": "prettier ./ --write --list-different",
    "lint": "prettier ./ --check"
  },
  "author": "Tatty",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "1.37.1",
    "dotenv": "16.3.1"
  },
  "devDependencies": {
    "prettier": "3.0.3"
  }
}
